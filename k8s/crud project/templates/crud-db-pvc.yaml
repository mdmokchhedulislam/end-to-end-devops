apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: crud-mongo-pvc
spec:
  accessModes:
    - {{ .Values.db.storage.accessMode }}
  resources:
    requests:
      storage: {{ .Values.db.storage.size }}
